#include <stdio.h>
#include <stdlib.h>

/* 3 main data types in c
 *
 * integer, double, character
 * 
 * There is additional 'datatype' called string
 * which is simply a collection of datatypes connected together
 */

int main()
{
    int age = 40;

    double gpa = 3.6;

    // Storing a single character using ''
    char grade = 'A';

    return 0;
}
