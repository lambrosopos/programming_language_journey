#include <stdio.h>
#include <stdlib.h>

int main()
{
    // Storing multiple characters in c -> making an array of char
    char characterName[] = "Spongebob";

    // Storing numbers -> use integers
    int characterAge = 14;

    printf("There once was a man named %s\n", characterName);
    printf("he was %d years old\n", characterAge);
    printf("He really like balloons\n");
    printf("but liked to play with Patrick.\n");

    return 0;
}
